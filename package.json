{
  "name": "attrecto-academy-react-mock",
  "version": "0.0.1",
  "description": "Attrecto Academy React mock",
  "main": "index.js",
  "scripts": {
    "build:production": "echo \"Skipping build stage, because we don't need it... \"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto-js": "^4.0.0",
    "husky": "^4.3.0",
    "jsonwebtoken": "^8.5.1",
    "prettier": "^2.1.2",
    "pretty-quick": "^3.1.0"
  },
  "devDependencies": {
    "json-server": "^0.16.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
